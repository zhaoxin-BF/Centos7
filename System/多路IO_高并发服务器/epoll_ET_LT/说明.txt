ET:边沿触发       只提醒一次，接没接收完跟我没关系,第二次就会一样阻塞，不会再次通知服务器处理未完的数据，会导致数据堆积。
 
LT:水平触发       一直提醒，没接收完一直提醒接收


结论：ET要比LT要好，epollET+IO非阻塞是最好的、最有效率的方法。
    epoll的ET、LT最优的是ET。
    read设置的非阻塞。        write(read()) fcntl(O_NOBLOCK);             
    强强联合是最最最最优的结果
